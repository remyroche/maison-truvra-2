// website/admin/js/admin_config.js
// Configuration for the Admin Panel application

// API base URLs for backend communication
const API_ADMIN_BASE_URL = 'http://127.0.0.1:5001/api/admin'; // Specific admin base
const API_BASE_URL = 'http://127.0.0.1:5001/api'; // For general API calls (e.g., fetching products for select lists)

// Note: If these constants are used across multiple admin JS files,
// ensure this script is loaded first, or consider using ES6 modules if your setup supports it.
// For this refactoring, we assume global availability after script loading order.
